---
phase: 05-setup-tooling
plan: 05
type: execute
wave: 4
depends_on: ["05-01", "05-03", "05-04"]
files_modified:
  - src/setup/SetupApp.tsx
  - src/setup/SetupApp.css
  - src/setup/FileDrop.tsx
  - src/setup/FileDrop.test.tsx
  - src/setup/ReviewCanvas.tsx
  - src/setup/ReviewCanvas.css
  - src/setup/ReviewCanvas.test.tsx
  - src/setup/LivePreview.tsx
  - src/setup/LivePreview.test.tsx
  - src/setup/dupWarning.ts
autonomous: false
requirements: [TOOL-01, TOOL-04]
tags: [review-ui, file-drop, pointer-events, live-preview, strictmode, portal, TOOL-01, TOOL-04]
must_haves:
  truths:
    - "src/setup/FileDrop.tsx exports a FileDrop component with drag-drop + <input type=file accept='image/png,image/jpeg,image/webp,image/avif'> (Pitfall 7 whitelist)"
    - "FileDrop calls createImageBitmap(file, { resizeWidth: 3000, resizeQuality: 'high' }) capped at 3000px (Pitfall 4)"
    - "FileDrop calls onImageReady(bitmap, file.name, imageUrl) where imageUrl is a URL.createObjectURL(file) — the setup owner handles URL.revokeObjectURL lifecycle"
    - "FileDrop surfaces decode errors with the App.tsx error-card visual pattern (error copy + Retry button) — Pitfall 7 fallback"
    - "src/setup/SetupApp.tsx becomes the state owner: uploadedBitmap, uploadedFileName, uploadedImageUrl, draftPins (DraftPin[]), status (PipelineProgress | null), selectedPinId (string | null), mode ('idle' | 'detecting' | 'review' | 'approved') — all via useState"
    - "SetupApp 'Detect tables' button is disabled while mode==='detecting' (single-flight guard — Pitfall 2)"
    - "SetupApp's onDetect click handler calls runDetectionPipeline(bitmap, fileName, setStatus) — DOES NOT use useEffect for pipeline init (Pitfall 3 — StrictMode-safe)"
    - "SetupApp revokes the object URL in a useEffect cleanup when the uploaded image changes — no leaked blobs"
    - "src/setup/ReviewCanvas.tsx renders the uploaded image scaled down (max-height 60vh on desktop) with absolute-positioned pin overlays whose left/top are pin.x*100% / pin.y*100%"
    - "Pin rendering respects D-11: status='ok' → red teardrop (reuse .pin-assigned SVG inline via a shared subcomponent or class — NO duplication of the SVG path; factor out if needed), status='low-confidence' → orange outline variant, status='needs-number' → slate '?' variant"
    - "ReviewCanvas supports DRAG (D-12): pointerdown on pin → pointermove updates pin.x/pin.y to cursor fraction → pointerup commits. Uses Pointer Events (covers mouse+touch+pen)"
    - "ReviewCanvas supports CLICK-TO-EDIT (D-12): click pin opens inline <input type='text' inputMode='numeric' pattern='[0-9]*'> seeded with pin.tableNumber; Enter or blur commits the override, Escape cancels"
    - "ReviewCanvas supports DELETE (D-12): 'x' button on each pin's hover state; Backspace/Delete key when a pin is selected (selectedPinId)"
    - "ReviewCanvas supports ADD (D-12): Shift+click OR toggleable 'Add pin' mode on empty canvas creates a new DraftPin at the click fraction, tableNumber=null, status='needs-number', admin types the number inline immediately"
    - "src/setup/dupWarning.ts exports findDuplicatePositions(pins, threshold=0.03): {pinId: string, otherPinId: string, distance: number}[] — Euclidean in 0..1 fraction space (D-14)"
    - "ReviewCanvas renders a visible inline warning listing duplicate pairs via findDuplicatePositions (D-14 — Phase 1 dev warning repurposed into user-facing UI since admins, not dev-console viewers, need it here)"
    - "src/setup/LivePreview.tsx wraps <FloorPlan config={buildSyntheticConfig(draftPins,fileName)} imageSrc={objectUrl}/> in <TransformWrapper initialScale=1 minScale=1 maxScale=4 centerOnInit>...</TransformWrapper> (D-13 + plan 05-03 interface)"
    - "LivePreview memoizes the synthetic FloorPlanConfig with useMemo(() => buildSyntheticConfig(pins, name), [pins, name]) so FloorPlan's DEV dup-warning dedupe works (plan 05-03 warnedConfigs ref gate)"
    - "LivePreview's focusedTableNumber prop lets SetupApp preview 'what this guest would see' — defaults to '' (show no pin) unless admin picks one"
    - "SetupApp dismounts/remounts the review UI on a re-upload (new bitmap → draftPins reset to []) — prevents stale pins on new image"
    - "React StrictMode double-mount does not spawn duplicate pipelines — Pitfall 3 enforced because init happens in onClick, not useEffect (RESEARCH §Pitfall 3)"
    - "Tests cover: FileDrop accept-list + oversize downscale; ReviewCanvas drag-move-commit; ReviewCanvas click-to-edit-Enter; ReviewCanvas delete via button; ReviewCanvas add via shift-click; dupWarning threshold cases; LivePreview renders FloorPlan with synthetic config"
  artifacts:
    - path: "src/setup/FileDrop.tsx"
      provides: "Upload UI (drag/drop + input) with whitelist + resize"
      contains: "createImageBitmap"
      min_lines: 60
    - path: "src/setup/FileDrop.test.tsx"
      provides: "Accept-list reject + resize kick-in"
      contains: "FileDrop"
      min_lines: 40
    - path: "src/setup/ReviewCanvas.tsx"
      provides: "Pin overlay + drag/edit/delete/add interactions + dup warning"
      contains: "pointermove"
      min_lines: 180
    - path: "src/setup/ReviewCanvas.css"
      provides: "Pin color variants (ok/low-confidence/needs-number) + hover delete button + add-mode crosshair cursor"
      contains: "review-"
      min_lines: 80
    - path: "src/setup/ReviewCanvas.test.tsx"
      provides: "Interaction specs: drag, edit, delete, add, dup warning visible"
      contains: "pointerdown"
      min_lines: 100
    - path: "src/setup/LivePreview.tsx"
      provides: "Wraps FloorPlan with synthetic config + TransformWrapper"
      contains: "buildSyntheticConfig"
      min_lines: 40
    - path: "src/setup/LivePreview.test.tsx"
      provides: "Renders FloorPlan with pins → coordinates propagate"
      contains: "buildSyntheticConfig"
      min_lines: 40
    - path: "src/setup/dupWarning.ts"
      provides: "findDuplicatePositions(pins, threshold) — pure function"
      contains: "findDuplicatePositions"
      min_lines: 25
    - path: "src/setup/SetupApp.tsx"
      provides: "Root owner — upload state + detect handler + review panes + checkpoint scaffold for approve (plan 05-06)"
      contains: "runDetectionPipeline"
      min_lines: 120
    - path: "src/setup/SetupApp.css"
      provides: "Two-pane review layout (review canvas left / live preview right on desktop; stacked on mobile)"
      contains: "setup-review"
      min_lines: 60
  key_links:
    - from: "src/setup/SetupApp.tsx"
      to: "src/setup/pipeline.ts::runDetectionPipeline"
      via: "async call inside onDetect handler (NOT in useEffect)"
      pattern: "runDetectionPipeline"
    - from: "src/setup/ReviewCanvas.tsx"
      to: "src/setup/dupWarning.ts::findDuplicatePositions"
      via: "named import rendered inline"
      pattern: "findDuplicatePositions"
    - from: "src/setup/LivePreview.tsx"
      to: "src/components/FloorPlan.tsx (default + FloorPlanConfig)"
      via: "import FloorPlan, { type FloorPlanConfig } — plan 05-03 contract"
      pattern: "from '../components/FloorPlan'"
    - from: "src/setup/LivePreview.tsx"
      to: "react-zoom-pan-pinch"
      via: "TransformWrapper + TransformComponent (existing dep)"
      pattern: "react-zoom-pan-pinch"
---

<objective>
Build the review UI: admin uploads → clicks Detect → pipeline runs → draft pins overlay the image → admin edits/drags/adds/deletes → live preview (real `<FloorPlan/>`) mirrors state. This plan is the largest in Phase 5 because the review UI is where correctness lives (RESEARCH.md §Summary fact #2). Approve + export is split out to plan 05-06 to keep this plan at ~50% context.

Purpose: Delivers the admin-facing side of **TOOL-01** (coordinate mappings via edit UI) and **TOOL-04** (auto-detect draft pins presented for review). Honors D-10, D-11, D-12, D-13, D-14.

Output: 8 new files in `src/setup/` + edits to SetupApp.tsx / SetupApp.css from plan 05-01. Ships a `checkpoint:human-verify` so the admin can confirm the real end-to-end flow works on the Reception Seat Diagram before plan 05-06 builds export on top.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-setup-tooling/05-CONTEXT.md
@.planning/phases/05-setup-tooling/05-RESEARCH.md
@.planning/phases/05-setup-tooling/05-01-PLAN.md
@.planning/phases/05-setup-tooling/05-03-PLAN.md
@.planning/phases/05-setup-tooling/05-04-PLAN.md
@src/components/FloorPlan.tsx
@src/components/FloorPlan.css
@src/components/MapView.tsx
@src/setup/SetupApp.tsx
@src/setup/types.ts
@src/setup/pipeline.ts
@CLAUDE.md

<interfaces>
Produced by this plan:
```ts
// src/setup/dupWarning.ts
export interface DupPair { pinId: string; otherPinId: string; distance: number; }
export function findDuplicatePositions(pins: DraftPin[], threshold?: number): DupPair[];

// src/setup/FileDrop.tsx
export interface FileDropProps {
  onImageReady: (bmp: ImageBitmap, fileName: string, objectUrl: string) => void;
  onError: (message: string) => void;
}
export default function FileDrop(props: FileDropProps): JSX.Element;

// src/setup/ReviewCanvas.tsx
export interface ReviewCanvasProps {
  imageUrl: string;
  imageNaturalWidth: number;
  imageNaturalHeight: number;
  pins: DraftPin[];
  selectedPinId: string | null;
  onChange: (next: DraftPin[]) => void;
  onSelect: (pinId: string | null) => void;
  disabled?: boolean;  // true in 'approved' mode (plan 05-06)
}
export default function ReviewCanvas(props: ReviewCanvasProps): JSX.Element;

// src/setup/LivePreview.tsx
export function buildSyntheticConfig(pins: DraftPin[], imageFileName: string): FloorPlanConfig;
export interface LivePreviewProps {
  pins: DraftPin[];
  imageUrl: string;
  imageFileName: string;
  focusedTableNumber?: string;
}
export default function LivePreview(props: LivePreviewProps): JSX.Element;
```

Consumed by plan 05-06 (approve + export):
```ts
import { buildSyntheticConfig } from './LivePreview';
// 06 passes pins + name into buildFloorPlanConfig (exportConfig.ts — same shape),
// but keeps LivePreview rendering in 'approved' mode by passing disabled=true down to ReviewCanvas.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/setup/FileDrop.tsx + test + dupWarning.ts + test — lightweight building blocks</name>
  <files>src/setup/FileDrop.tsx, src/setup/FileDrop.test.tsx, src/setup/dupWarning.ts, src/setup/dupWarning.test.ts</files>
  <behavior>
    FileDrop.tsx:
    - Render a `<label className="setup-file-drop">` containing a visible drop zone and a hidden `<input type="file" accept="image/png,image/jpeg,image/webp,image/avif" />`.
    - Support drag/drop (onDragOver preventDefault, onDrop reads e.dataTransfer.files[0]).
    - On file:
      1. Reject if type not in the accept whitelist (Pitfall 7).
      2. `const objectUrl = URL.createObjectURL(file);`
      3. `const bitmap = await createImageBitmap(file, { resizeWidth: 3000, resizeQuality: 'high' });` — BUT only apply resizeWidth if `file size > ~3MB OR decoded width would exceed 3000`; for small clean PNGs leave native resolution. Simplest approach: always pass `resizeWidth` with the min(naturalWidth, 3000) cap — createImageBitmap handles it.
      4. `onImageReady(bitmap, file.name, objectUrl);`
      5. On decode error: call onError with "This image format isn't supported. Please upload a PNG, JPEG, WebP, or AVIF." — copy from Pitfall 7.

    FileDrop.test.tsx:
    1. Renders drop-zone + hidden input.
    2. Rejects .heic upload with the expected error copy.
    3. On PNG upload: onImageReady is called with the bitmap-like object + filename + objectUrl string.
    4. On throw from createImageBitmap: onError fires with Pitfall-7 copy.

    dupWarning.ts:
    - `findDuplicatePositions(pins, threshold = 0.03): DupPair[]` — O(n²) scan (54 pins × 54 = 2916 compares, negligible).
    - Returns unique pairs (pinId < otherPinId order) where Euclidean fraction distance < threshold.

    dupWarning.test.ts:
    1. Empty array → [].
    2. Two pins at identical coords → 1 pair.
    3. Two pins 0.04 apart with default threshold → 0 pairs.
    4. Two pins 0.02 apart → 1 pair.
    5. Three pins all within threshold → 3 pairs (n choose 2).
    6. Custom threshold override.
  </behavior>
  <read_first>
    - .planning/phases/05-setup-tooling/05-RESEARCH.md §Code Examples "Upload → decoded image", §Pitfall 7 (accept list)
    - .planning/phases/05-setup-tooling/05-CONTEXT.md D-14 (dup warning)
    - src/App.tsx (error-card visual pattern — matches tone)
  </read_first>
  <action>
    Implement per <behavior>. For FileDrop.tsx visual: keep copy minimal and obvious — "Drop floor-plan image here or click to choose". Mirror the upload-button placeholder in SetupApp.tsx from plan 05-01.

    For createImageBitmap in jsdom test env: it's not implemented. Mock it via `vi.stubGlobal('createImageBitmap', vi.fn(async () => ({ width: 2400, height: 1831, close: () => {} })))` in the test.

    For URL.createObjectURL in jsdom: same — stub with `vi.stubGlobal('URL', { createObjectURL: vi.fn(() => 'blob:fake'), revokeObjectURL: vi.fn() })`.
  </action>
  <verify>
    <automated>npm test -- src/setup/FileDrop src/setup/dupWarning</automated>
  </verify>
  <done>
    - 10 specs pass (4 FileDrop + 6 dupWarning)
    - tsc + lint clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/setup/ReviewCanvas.{tsx,css,test.tsx} + LivePreview.{tsx,test.tsx} — the review + preview surfaces</name>
  <files>src/setup/ReviewCanvas.tsx, src/setup/ReviewCanvas.css, src/setup/ReviewCanvas.test.tsx, src/setup/LivePreview.tsx, src/setup/LivePreview.test.tsx</files>
  <behavior>
    ReviewCanvas.tsx:
    - Container `<div className="review-canvas" ref={containerRef}>`. Inside: `<img src={imageUrl} className="review-canvas-image" draggable={false} onLoad={…}/>` + absolute-positioned pin overlays.
    - Pin: `<button type='button' className={`review-pin review-pin--${pin.status}${pin.id === selectedPinId ? ' is-selected' : ''}`} style={{left: `${pin.x*100}%`, top: `${pin.y*100}%`}}>` with:
      - SVG teardrop (red fill) for status='ok'
      - SVG teardrop (transparent fill, orange stroke) for status='low-confidence'
      - Slate circle with '?' for status='needs-number'
      - Number label (or '?') inside
      - Hover-visible '×' delete button in the top-right corner
    - DRAG: pointerdown on pin → captures pointer → pointermove computes (e.clientX - containerRect.left) / containerRect.width and same for y → setState with updated pin → pointerup releases.
    - CLICK (no drag): becomes "select + open inline editor" on very short pointerdown/up delta. Editor is an `<input type='text' inputMode='numeric' pattern='[0-9]*'>` positioned near the pin. Enter commits, Escape cancels, blur commits.
    - DELETE: '×' button click removes the pin. Keyboard Backspace/Delete when pin selected (tabindex=0 on pin button).
    - ADD: `<button className='review-addmode-toggle'>Add pin</button>` toggles a mode. When mode is on and container is clicked (not on a pin), create a new DraftPin at fraction coordinates, status='needs-number', tableNumber=null, open the inline editor immediately. Alternative: Shift+click also triggers add (documented inline).
    - Dup warning strip: above the canvas, render `findDuplicatePositions(pins).map(…)` as a styled notice: "Pins {X} and {Y} are within 3% — this usually means a detection duplicate. Consider deleting one."
    - `disabled` prop disables all interaction (used when mode==='approved').

    ReviewCanvas.css:
    - `.review-canvas` position:relative; max-height:60vh; overflow:auto (image may overflow at desktop widths).
    - `.review-canvas-image` max-width:100%; height:auto; user-select:none; -webkit-user-drag:none.
    - `.review-pin` position:absolute; transform:translate(-50%, -100%) (teardrop anchors at its tip).
    - Status color variants (.review-pin--ok uses existing .pin-assigned palette; --low-confidence orange; --needs-number slate).
    - `.review-pin.is-selected` adds a ring outline.
    - `.review-dup-warning` above the canvas: orange bg, rounded, readable.
    - Mobile @media (max-width: 600px) stacks preview below canvas.

    ReviewCanvas.test.tsx (use @testing-library/user-event for pointer events):
    1. Renders all pins with correct left/top percentages.
    2. Pointerdown on a pin + pointermove + pointerup → onChange called with updated pin.x/pin.y.
    3. Click on a pin → input appears. Type '99' + Enter → onChange with tableNumber='99'. Status transitions 'needs-number' → 'ok'.
    4. '×' button click → onChange called with pin removed.
    5. Add-mode: toggle Add button, click on canvas at 50%/50% → onChange called with a new pin at x=0.5, y=0.5, status='needs-number'.
    6. findDuplicatePositions warning renders when pins contain a close pair.
    7. disabled=true: pointerdown does nothing.

    LivePreview.tsx:
    - `buildSyntheticConfig(pins, fileName): FloorPlanConfig` — maps pins to { imageFileName: fileName, tablePositions: Record<string, {x,y}> }. Skips pins where tableNumber is null. Preserves duplicates (last-one-wins; the duplicate warning tells admin to fix).
    - Component: `<TransformWrapper initialScale={1} minScale={1} maxScale={4} centerOnInit doubleClick={{disabled: true}}><TransformComponent wrapperClass='live-preview-wrapper'><FloorPlan tableNumber={focusedTableNumber ?? ''} assignedPinRef={noopRef} onImageLoad={() => {}} config={useMemo(…)} imageSrc={imageUrl}/></TransformComponent></TransformWrapper>`
    - `noopRef = () => {}` (ref-callback form, not useRef).

    LivePreview.test.tsx:
    1. buildSyntheticConfig converts 3 pins with tableNumber → FloorPlanConfig with 3 entries.
    2. buildSyntheticConfig skips tableNumber:null pins.
    3. LivePreview renders FloorPlan (querySelector `.floor-plan-wrapper`) — needs TransformWrapper context; wrap in MemoryRouter-free bare render since it provides its own TransformWrapper internally.
    4. focusedTableNumber propagates to FloorPlan — pin for that number renders with .pin-assigned class.
  </behavior>
  <read_first>
    - .planning/phases/05-setup-tooling/05-RESEARCH.md §Pattern 5 (LivePreview code sample)
    - .planning/phases/05-setup-tooling/05-CONTEXT.md D-11, D-12, D-13, D-14
    - src/components/FloorPlan.tsx (post-plan-05-03 — new config + imageSrc props)
    - src/components/FloorPlan.css (.pin-assigned reused; same red teardrop path as ReviewCanvas status='ok')
    - src/components/MapView.tsx (TransformWrapper usage pattern + centerOnInit semantics)
  </read_first>
  <action>
    ReviewCanvas.tsx implementation notes:
    - Drag distinction: on pointerdown, record startX/startY + time. On pointerup within <150ms AND <4px movement → treat as click (open editor). Otherwise treat the pointermove stream as a drag and commit final x/y on up.
    - Use `e.currentTarget.setPointerCapture(e.pointerId)` on pointerdown to avoid pointer-leave-during-drag issues on mobile.
    - For add-mode: keep the toggle visually pressed when active; show a crosshair cursor on the canvas (`cursor: crosshair` via class).

    LivePreview.tsx: the synthetic config must have imageFileName set to the uploaded file's name (plan 05-06 reads this back for the downloadable JSON's imageFileName field). If admin uploaded "wedding-layout.png" that's what ends up in floorPlan.json's imageFileName — matches D-16.

    Keep the review canvas and preview independent — admin can edit pins without re-running detection. LivePreview only re-memoizes when pins or fileName change.

    One subtle bug to avoid (flagged in CLAUDE.md conventions): noUnusedLocals under strict means `noopRef` must be declared outside render or passed through. Declare as const at module scope: `const noopRef: React.Ref<HTMLDivElement> = () => {};`.
  </action>
  <verify>
    <automated>npm test -- src/setup/ReviewCanvas src/setup/LivePreview</automated>
  </verify>
  <done>
    - 11 specs pass (7 ReviewCanvas + 4 LivePreview)
    - tsc + lint clean
    - No usage of useEffect for init work in either component (StrictMode guard)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire SetupApp.tsx — upload → detect → review two-pane layout; replace plan 05-01's placeholder</name>
  <files>src/setup/SetupApp.tsx, src/setup/SetupApp.css</files>
  <behavior>
    SetupApp.tsx state:
    - `uploadedBitmap: ImageBitmap | null`
    - `uploadedFileName: string | null`
    - `uploadedImageUrl: string | null`
    - `draftPins: DraftPin[]` (default [])
    - `selectedPinId: string | null`
    - `status: PipelineProgress | null`
    - `mode: 'idle' | 'detecting' | 'review'` (approved comes in plan 05-06)
    - `error: string | null`

    Layout (idle):
    - Title + D-04 obscurity warning (from plan 05-01, kept)
    - <FileDrop onImageReady onError/>

    Layout (image uploaded, pre-detect):
    - Thumbnail of uploaded image + filename
    - Big "Detect tables" button + status line (empty until clicked)

    Layout (detecting):
    - Status line: "{status.message}{status.done != null ? ` (${status.done}/${status.total})` : ''}"
    - Detect button disabled (Pitfall 2 single-flight)

    Layout (review):
    - Two-pane (CSS grid, stacked on mobile):
      - Left: <ReviewCanvas imageUrl imageNaturalWidth={bitmap.width} imageNaturalHeight={bitmap.height} pins={draftPins} selectedPinId onChange={setDraftPins} onSelect={setSelectedPinId}/>
      - Right: <LivePreview pins={draftPins} imageUrl={uploadedImageUrl} imageFileName={uploadedFileName ?? 'floor-plan'} focusedTableNumber={selectedPinNumber}/>
    - Summary bar above: "Found {N} tables. Review, correct, then approve below." + pin-status counts (ok / low-confidence / needs-number)
    - Approve button placeholder (disabled; hooks up in plan 05-06)
    - Re-upload button (resets all state)

    Layout (error):
    - App-style error card with Retry button + error copy.

    Handlers:
    - `onImageReady(bmp, name, url)`: revoke previous url if present; setUploadedBitmap/FileName/ImageUrl; draftPins=[]; mode='idle'; error=null.
    - `onDetect()`:
        if (!uploadedBitmap) return;
        setMode('detecting');
        setError(null);
        try {
          const pins = await runDetectionPipeline(uploadedBitmap, uploadedFileName!, setStatus);
          setDraftPins(pins);
          setMode('review');
        } catch (e) {
          setError(e instanceof Error ? e.message : 'Detection failed');
          setMode('idle');
        }
    - useEffect(() => () => { if (uploadedImageUrl) URL.revokeObjectURL(uploadedImageUrl); }, [uploadedImageUrl]) — cleanup on unmount OR URL change.

    CSS additions:
    - `.setup-review-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }`
    - `@media (max-width:900px) { .setup-review-grid { grid-template-columns: 1fr; } }`
    - Status line + thumbnail styling.

    Pitfall 3 adherence (StrictMode):
    - DO NOT call runDetectionPipeline in a useEffect. It runs inside the onClick handler. Module-level cvPromise (plan 05-04) ensures second mount reuses the init, no double-worker.
  </behavior>
  <read_first>
    - src/setup/SetupApp.tsx (plan 05-01 shell — now replaced)
    - src/setup/pipeline.ts, FileDrop.tsx, ReviewCanvas.tsx, LivePreview.tsx (earlier tasks of this plan)
    - src/App.tsx (error card pattern)
    - .planning/phases/05-setup-tooling/05-RESEARCH.md §Pitfall 2 (single-flight Detect), §Pitfall 3 (StrictMode-safe init)
  </read_first>
  <action>
    Rewrite SetupApp.tsx per <behavior>. Keep the D-04 obscurity warning from plan 05-01 visible at all times in the header.

    For Tesseract's status callback: pipeline.ts (plan 05-04) emits PipelineProgress with stage + message. During the ocr stage it also passes done/total. Render the status line as:
    `{status.message}{status.done != null && status.total != null ? ` (${status.done}/${status.total})` : ''}`

    Re-upload: pressing the re-upload button sets mode='idle', clears draftPins, revokes the current object URL, resets uploadedBitmap to null. The file input re-appears.

    Leave the Approve button visible but `disabled` in this plan — plan 05-06 wires it.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint -- src/setup && npm test -- src/setup</automated>
  </verify>
  <done>
    - SetupApp.tsx compiles + lints clean
    - tsc + lint clean overall
    - All setup tests from plans 05-02, 05-04, 05-05 green
    - A `checkpoint:human-verify` gate (Task 4) confirms the real flow works on the Reception Seat Diagram
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verifies end-to-end review flow on the real Reception Seat Diagram</name>
  <what-built>
    SetupApp.tsx now supports full upload → detect → review. No approve/export yet (plan 05-06). Drag, edit, delete, add pin all work. LivePreview (real FloorPlan) mirrors state.
  </what-built>
  <how-to-verify>
    1. `npm run dev`
    2. Visit http://localhost:5173/setup
    3. Upload `public/floor-plan/floor-plan-1600.png` (or `src/assets/Reception Seat Diagram.png`).
    4. Click "Detect tables". Watch the status line progress through: Preparing → Scanning → Cropping → Reading (1/N → N/N) → Done. Should take 5-15s on first run (tesseract warmup).
    5. Confirm draft pins overlay the image. Compare against src/config/floorPlan.json's 54 tables — expect recall ≥ 83% per plan 05-02 calibration.
    6. Drag a pin: verify pointermove updates position, LivePreview shows the pin moving in sync.
    7. Click a pin with a wrong/missing number: inline editor appears → type a number → Enter commits.
    8. Delete a false positive: hover → click '×'. Pin disappears in both panes.
    9. Add a missed table: toggle "Add pin" → click on an empty circle in the image → editor opens → type number.
    10. Force a duplicate warning: drag two pins within 3% of each other. Warning strip above the canvas lists the pair.
    11. Reload the page (admin navigates). Confirm no PWA service-worker interference stuck a stale chunk — if so, file a follow-up note; the PWA sw.js covers '/' primarily.
    12. Approve this plan once you can navigate the full review loop without any surprises.
  </how-to-verify>
  <files>(no files — human-gated checkpoint; review artifacts produced by prior tasks)</files>
  <action>Manually run the 12-step end-to-end flow on the real Reception Seat Diagram per the `how-to-verify` list. Record any UX bugs as follow-up notes in 05-05-SUMMARY.md.</action>
  <verify>Manual smoke via npm run dev + /setup — see how-to-verify steps</verify>
  <done>All 12 verification steps pass without surprises; admin can navigate upload → detect → drag/edit/delete/add → dup-warning loop cleanly.</done>
  <resume-signal>Type "review-flow-works" (or describe bugs).</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npm run lint` clean
- `npm test -- src/setup` green (all specs from 05-02 + 05-04 + 05-05)
- `npm test` overall green
- Manual end-to-end flow via Task 4 checkpoint
- Grep: `grep -rn 'tesseract\|opencv' src/ --include='*.ts' --include='*.tsx'` returns matches ONLY inside src/setup/
- Grep: `grep -rn 'useEffect.*runDetectionPipeline\|useEffect.*createWorker\|useEffect.*getCv' src/setup/` returns nothing (Pitfall 3 enforced)
</verification>

<success_criteria>
- Upload, detect, review, drag, edit, delete, add all work on the real PNG
- LivePreview mirrors pin state via the unmodified FloorPlan component
- Dup warning fires for close pins
- StrictMode double-mount does not spawn duplicate pipelines (module singletons in plan 05-04)
- Approve button is visible but disabled — plan 05-06 wires it
- No guest-code file touched this plan (FloorPlan.tsx was widened in plan 05-03)
</success_criteria>

<output>
Create `.planning/phases/05-setup-tooling/05-05-SUMMARY.md` documenting:
- Recall observed on the real PNG vs calibration expectation (plan 05-02)
- Any pin-interaction UX tweaks added beyond the plan (record them)
- Confirmation the StrictMode double-mount is clean (no duplicate workers in dev tools)

Commit: `feat(05): review UI — FileDrop, ReviewCanvas, LivePreview, SetupApp wiring (TOOL-01, TOOL-04, D-10..D-14)`
</output>
