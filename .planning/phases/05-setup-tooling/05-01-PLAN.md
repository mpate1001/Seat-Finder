---
phase: 05-setup-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.tsx
  - src/Root.tsx
  - src/setup/SetupApp.tsx
  - src/setup/SetupApp.css
  - src/setup/index.ts
  - src/main.test.tsx
autonomous: true
requirements: [TOOL-03]
tags: [route-split, react-lazy, suspense, setup-shell, strictmode, TOOL-03]
must_haves:
  truths:
    - "src/main.tsx dispatches on window.location.pathname: '/setup' renders <SetupApp/> (lazy), everything else renders <App/> (D-01, D-02)"
    - "src/main.tsx imports SetupApp exclusively via `lazy(() => import('./setup/SetupApp'))` — zero static imports from src/setup/ anywhere in the guest graph (D-01)"
    - "src/main.tsx wraps the lazy SetupApp in <Suspense fallback={...}> so initial chunk fetch is visible to the admin"
    - "pathname read happens ONCE at module eval — not inside an effect, not inside a listener (D-02 — no navigation listening, reload-to-navigate)"
    - "src/setup/SetupApp.tsx is a default-exported function component named SetupApp"
    - "SetupApp renders a visible <h1>Setup tool</h1>, the subtitle 'Floor-plan admin — route obscurity only, DO NOT share this URL' (D-04), and an obvious upload button placeholder ('Upload floor plan' — wired for real in plan 05-05)"
    - "SetupApp contains NO imports of @techstark/opencv-js or tesseract.js — those land in 05-04 behind further lazy-init inside handlers"
    - "SetupApp contains NO useEffect that races StrictMode double-invoke — the shell has no init side effects (Pitfall 3)"
    - "src/setup/SetupApp.css holds all setup-shell styling with the .setup- prefix convention (mirrors .floor-plan-, .map-view- patterns)"
    - "src/setup/index.ts is a barrel that re-exports SetupApp as default (so import('./setup/SetupApp') resolves through the directory root if desired)"
    - "src/main.test.tsx has a test that mounts the Root dispatcher with window.location.pathname='/setup' and verifies the Suspense fallback copy renders"
    - "src/main.test.tsx has a test that mounts the Root dispatcher with window.location.pathname='/' and verifies the guest <App/> renders (no 'Setup tool' heading)"
    - "Running `npm run lint` is clean after this plan"
    - "Running `npm test -- main` is green after this plan"
  artifacts:
    - path: "src/main.tsx"
      provides: "Pathname dispatch between guest App and lazy SetupApp under a single <StrictMode>/<Suspense> tree"
      contains: "React.lazy"
      min_lines: 25
    - path: "src/setup/SetupApp.tsx"
      provides: "Setup tool shell — default export, no CV/OCR deps, no init effects"
      contains: "Setup tool"
      min_lines: 30
    - path: "src/setup/SetupApp.css"
      provides: "Shell layout (centered card over the existing body background — matches guest app visual tone)"
      contains: "setup-"
      min_lines: 20
    - path: "src/setup/index.ts"
      provides: "Directory barrel (default re-export of SetupApp)"
      contains: "SetupApp"
      min_lines: 1
    - path: "src/main.test.tsx"
      provides: "2 specs locking the dispatch contract"
      contains: "setup"
      min_lines: 35
  key_links:
    - from: "src/main.tsx"
      to: "src/setup/SetupApp"
      via: "React.lazy dynamic import — the ONLY edge from guest graph into setup"
      pattern: "lazy\\(\\(\\) => import\\('\\./setup/SetupApp'\\)\\)"
    - from: "src/main.tsx"
      to: "window.location.pathname"
      via: "single top-level read inside Root() — no listener"
      pattern: "window.location.pathname"
    - from: "src/setup/SetupApp.tsx"
      to: "src/setup/SetupApp.css"
      via: "side-effect CSS import"
      pattern: "SetupApp.css"
---

<objective>
Split the single-entry Vite SPA into two routes: guest `<App/>` stays on `/`, and a lazy-loaded `<SetupApp/>` shell mounts on `/setup`. No router library. No runtime dependencies added by this plan. Creates the `src/setup/` directory boundary that every later Phase 5 plan operates inside.

Purpose: Delivers the **TOOL-03** scaffold — once this plan ships, Rollup's default chunking places `SetupApp` + its transitive deps in a separate chunk from the guest entry (confirmed by RESEARCH.md §1 and Vite discussion #17730). Plans 05-02..05-06 will load OpenCV + Tesseract inside this boundary; plan 05-07 grep-verifies the isolation.

Output: Modified `src/main.tsx` with pathname dispatch + `React.lazy` + `<Suspense>`; new `src/setup/SetupApp.{tsx,css}`; new `src/setup/index.ts` barrel; new `src/main.test.tsx` with 2 dispatch specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-setup-tooling/05-CONTEXT.md
@.planning/phases/05-setup-tooling/05-RESEARCH.md
@src/main.tsx
@src/App.tsx
@src/index.css
@CLAUDE.md

<interfaces>
Produced by this plan:
```tsx
// src/setup/SetupApp.tsx — default export
export default function SetupApp(): JSX.Element;

// src/setup/index.ts — optional barrel
export { default } from './SetupApp';
```

Consumed by `src/main.tsx`:
```tsx
const SetupApp = lazy(() => import('./setup/SetupApp'));
// inside Root(): if pathname === '/setup' => <Suspense fallback=...><SetupApp/></Suspense>
//                else                      => <App/>
```

Reserved for plan 05-05 (not wired here):
- Upload button is a visible placeholder; click handler becomes `onFile(file)` wired by plan 05-05 through `<FileDrop/>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add src/setup/SetupApp.{tsx,css} + src/setup/index.ts barrel (the lazy target)</name>
  <files>src/setup/SetupApp.tsx, src/setup/SetupApp.css, src/setup/index.ts</files>
  <behavior>
    - SetupApp is a default-exported function component returning JSX with:
      - A main landmark / wrapping `<div className="setup-app">`
      - `<h1>Setup tool</h1>`
      - Subtitle copy exactly matching D-04: "Floor-plan admin — route obscurity only, DO NOT share this URL."
      - A disabled-looking `<button className="setup-upload-button">Upload floor plan</button>` placeholder (no onClick yet)
      - A short help paragraph: "Upload a floor-plan image (PNG / JPEG / WebP / AVIF). The detection + review workflow lights up in a later step."
    - No useEffect. No subscribers. No imports of @techstark/opencv-js or tesseract.js.
    - No side-effect state writes at module scope (module-level singletons belong in later plans; the shell is declarative only).
    - CSS file defines .setup-app, .setup-app h1, .setup-app .setup-subtitle, .setup-upload-button — matches Seat-Finder palette (#2b2d42 navy, #ef233c accent, 10px/20px border radius, system font stack per CLAUDE.md conventions).
    - index.ts: `export { default } from './SetupApp';`
  </behavior>
  <read_first>
    - .planning/phases/05-setup-tooling/05-CONTEXT.md (D-01..D-04)
    - .planning/phases/05-setup-tooling/05-RESEARCH.md §Pattern 1 (Pathname dispatch with React.lazy) + §Project Structure
    - src/App.tsx (match visual tone — .card styling + subtitle pattern)
    - src/index.css (global reset + body font stack)
    - CLAUDE.md §Conventions (PascalCase components, kebab-case CSS with component prefix, function declarations)
  </read_first>
  <action>
    Create src/setup/SetupApp.tsx as a default-exported function component per RESEARCH.md §Pattern 1's Project Structure block.
    Create src/setup/SetupApp.css — the .setup-app container mirrors App.tsx's .card shape (white-ish panel, 20px radius, padding, soft shadow) but clearly labeled "Setup tool" in the h1. DO NOT reuse the wedding background image (background ref in App.tsx) — setup tool is utility, not ceremony; keep it on a plain #edf2f4 body.
    Create src/setup/index.ts: `export { default } from './SetupApp';`
    No useEffect, no state except what's needed for a static shell (none).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint -- src/setup</automated>
  </verify>
  <done>
    - src/setup/SetupApp.tsx default-exports SetupApp
    - src/setup/SetupApp.css loads via side-effect import from SetupApp.tsx
    - src/setup/index.ts re-exports default
    - tsc + eslint clean
    - No imports of @techstark/opencv-js or tesseract.js in this file (grep confirms)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract src/Root.tsx + rewrite src/main.tsx as a thin entry shim</name>
  <files>src/main.tsx, src/Root.tsx</files>
  <behavior>
    - **Split into two files so tests can import Root without triggering createRoot at module load (jsdom can't host a real #root element cleanly across suites).**
    - `src/Root.tsx` exports `default function Root(): JSX.Element`:
      - Reads `window.location.pathname` at render time (D-02 — no listener, no useEffect)
      - `/setup` → `<Suspense fallback={<div className="setup-loading">Loading setup tool…</div>}><SetupApp /></Suspense>` where `SetupApp = lazy(() => import('./setup/SetupApp'))` at module scope
      - Else → `<App />`
      - No useEffect; no side effects at module scope
    - `src/main.tsx` becomes a pure entry shim:
      - `import { StrictMode } from 'react'; import { createRoot } from 'react-dom/client'; import Root from './Root'; import './index.css';`
      - `createRoot(document.getElementById('root')!).render(<StrictMode><Root/></StrictMode>);`
      - Nothing else. No component body.
    - Tests import Root from `src/Root.tsx` — never from `src/main.tsx`.
    - No static import of './setup/SetupApp' or anything under './setup/' from any guest file — only the dynamic `lazy(() => import(...))` edge inside Root.tsx.
  </behavior>
  <read_first>
    - src/main.tsx (current shape — must preserve StrictMode + createRoot)
    - .planning/phases/05-setup-tooling/05-RESEARCH.md §Pattern 1 (drop-in example)
    - src/components/MapView.tsx lines 46-87 (StrictMode double-invoke precedent — Pitfall 3)
  </read_first>
  <action>
    Replace src/main.tsx with the §Pattern 1 code sample verbatim (adapted to match the existing App.tsx import path). Keep `import './index.css';`. The pathname check runs inline in the Root component body — NOT inside useEffect (intentional per D-02: reload-to-navigate means we never need to re-read pathname).

    Per D-01: the dynamic import is the ONLY allowed edge from guest graph into setup graph. Any future temptation to `import SetupApp from './setup/SetupApp'` at the top of a guest file must be rejected — RESEARCH.md §8 grep gate enforces this at build time (wired in plan 05-07).

    Fallback copy: `<div className="setup-loading">Loading setup tool…</div>` — keep it ASCII (single low-line '...') to avoid any bundler-sensitive characters leaking into the setup chunk match.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>
    - src/main.tsx contains `lazy(() => import('./setup/SetupApp'))` (single occurrence)
    - src/main.tsx contains `window.location.pathname === '/setup'` (single occurrence)
    - src/main.tsx has NO static import path starting with `./setup/` (grep confirms)
    - tsc + eslint clean
    - `npm run dev` + manual visit to `/` renders the guest app; `/setup` renders "Setup tool" heading (manual smoke — automation via Task 3)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add src/main.test.tsx locking the dispatch contract</name>
  <files>src/main.test.tsx</files>
  <behavior>
    - Test 1 "dispatches guest App on /": stub `window.location` with `{ pathname: '/' }`, render <Root/>, assert the guest loading card or its "Seat Finder" title renders.
    - Test 2 "dispatches SetupApp on /setup": stub `window.location` with `{ pathname: '/setup' }`, render <Root/>, assert the Suspense fallback "Loading setup tool..." is visible synchronously (the lazy chunk has not resolved in the test env — the fallback is the deterministic observable).
    - Test 3 "no static setup imports": use `fs.readFileSync('src/main.tsx', 'utf8')` and regex-assert there is no `import .* from '\\./setup/.*'` line (only the `lazy(() => import(...))` dynamic form is allowed).
    - Tests export a helper `renderRootAt(pathname: string)` for consistent location stubbing.
  </behavior>
  <read_first>
    - src/main.tsx (post-Task 2 shape)
    - src/components/UpdateToast.test.tsx OR src/App.test.tsx (existing vitest+@testing-library conventions)
    - vitest config (inline in vite.config — default jsdom env is fine)
  </read_first>
  <action>
    Create src/main.test.tsx. Use vitest `vi.stubGlobal('location', { pathname })` or `Object.defineProperty(window, 'location', {...})` inside a beforeEach. **Import Root from `src/Root.tsx`** (NOT from `src/main.tsx` — that would trigger `createRoot(#root)` at module load and crash jsdom). Mount with @testing-library/react.

    For Test 3: Read `src/main.tsx` from disk (`fs.readFileSync`) and regex-match. This is the simplest automation of the "no static setup imports" invariant; the plan 05-07 grep gate enforces the same at build time, but a unit-level guard catches regressions earlier. Also confirm that `src/Root.tsx` contains no static `from './setup/` imports.

    Do NOT import from './setup/SetupApp' at the top of main.test.tsx — use `const SetupApp = await import('./setup/SetupApp')` if the test ever needs to resolve the lazy chunk (Test 2 doesn't need to; the fallback is observable without resolving).
  </action>
  <verify>
    <automated>npm test -- src/main.test</automated>
  </verify>
  <done>
    - 3 specs pass
    - Running `npm test` overall stays green (no regressions)
    - Running `npm run build` still succeeds (tsc accepts the named `export function Root`)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — clean
- `npm run lint` — clean
- `npm test -- src/main src/setup` — green
- `npm run dev` + visit http://localhost:5173/ — existing guest app renders unchanged
- `npm run dev` + visit http://localhost:5173/setup — "Setup tool" heading visible; upload button is a visual placeholder (non-functional is correct for this plan)
- Grep: `grep -rn "from '\./setup/" src/ --include='*.tsx' --include='*.ts'` returns ONLY `src/main.tsx`'s dynamic-import line and `src/setup/` files themselves (no guest-graph file statically imports from setup/)
</verification>

<success_criteria>
- Guest app at `/` renders byte-identically to pre-plan (no visual regression)
- Setup shell at `/setup` renders "Setup tool" heading + D-04 subtitle copy
- `src/setup/` directory exists with SetupApp.tsx, SetupApp.css, index.ts
- `window.location.pathname === '/setup'` appears exactly once in src/main.tsx
- `lazy(() => import('./setup/SetupApp'))` appears exactly once in src/main.tsx
- 3 vitest specs in src/main.test.tsx pass
- No imports of opencv / tesseract in any file (future plans add them inside src/setup/)
</success_criteria>

<output>
Create `.planning/phases/05-setup-tooling/05-01-SUMMARY.md` documenting:
- Final diff against src/main.tsx
- SetupApp.tsx shape + placeholder upload button
- Dispatch contract (pathname once, no listener, StrictMode preserved)
- The `src/setup/` directory is now the boundary — plans 05-02..05-06 live entirely inside it
- Any surprises (e.g. whether Vite dev server required a config tweak for the new /setup route — it shouldn't; SPA fallback handles it)

Commit: `feat(05): route-split guest / setup with React.lazy + Suspense shell (TOOL-03 scaffold, D-01, D-02, D-04)`
</output>
